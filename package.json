{
  "name": "thewowdb-item-price-guide-preview",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4173",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "test": "node item/check-css.mjs"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
